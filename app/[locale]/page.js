// import { useTranslations } from "next-intl";

import Home from "./components/home";

export default function Index() {
  // const t = useTranslations("Text");
  // console.log(t("home_explanation"));
  return (
    <>
      <Home
      // language={t("language")}
      // brand={t("brand")}
      // html_title={t("html_title")}
      // html_mata_description={t("html_mata_description")}
      // home={t("home")}
      // FAQ={t("FAQ")}
      // contact={t("contact")}
      // about={t("about")}
      // home_slogan={t("home_slogan")}
      // home_slogan_p={t("home_slogan_p")}
      // start_button={t("start_button")}
      // coming_button={t("coming_button")}
      // author={t("author")}
      // publisher={t("publisher")}
      // publish_date={t("publish_date")}
      // book_description={t("book_description")}
      // text_snippet={t("text_snippet")}
      // question_text={t("question_text")}
      // click_text={t("click_text")}
      // server_error={t("server_error")}
      // limit_error={t("limit_error")}
      // home_explanation={t("home_explanation")}
      />
    </>
  );
}
